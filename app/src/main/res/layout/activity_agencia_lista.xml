<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_agencia_lista"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="0dp"
    tools:context="to.dtech.rotadasaguas.AgenciaListaActivity">

    <ImageView
        android:id="@+id/imagemSad"
        android:layout_gravity="center_horizontal"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_weight="1.19"
        android:layout_marginTop="25dp"
        android:src="@mipmap/ic_sad_face"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_marginTop="10dp"
        android:id="@+id/textoSad"
        android:textAlignment="center"
        android:layout_below="@id/imagemSad"
        android:text="Nenhuma agência cadastrada na cidade selecionada."
        android:layout_width="match_parent"
        android:lineSpacingExtra="10dp"
        android:layout_height="wrap_content" />

    <ListView
        android:id="@+id/lvAgenciasItens"
        style="@style/listaAgencias"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"></ListView>

</RelativeLayout>
